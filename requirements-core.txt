# Pinned CORE (tabular) dependencies for the reproducibility capsule / Docker image.
# Exact versions used for the paper so the CHD-49 example is reproducible.
# The heavy [image] stack (torch / torchvision / torchxrayvision / scikit-image /
# Pillow) is intentionally excluded — the capsule reproduces the CPU-only tabular
# example. For the full image experiments use `pip install ".[image]"` instead.
joblib==1.3.2
numpy==1.26.4
pandas==2.0.3
scikit_learn==1.2.2
scipy==1.12.0
